# raw/ — Immutable Source Inputs

Drop your source documents here: articles, notes, transcripts, PDFs, images (in `assets/`).

**Rule:** the LLM agent **reads** these but **never edits or deletes** them. This folder is the
source of truth. Everything in `wiki/` is derived from — and cites back to — files here.
